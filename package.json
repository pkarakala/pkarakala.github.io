{
  "name": "pranav-reddy-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build && node scripts/check-build.mjs",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check",
    "check:links": "node scripts/check-external-links.mjs",
    "resume:set": "node scripts/replace-resume.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "astro": "^7.3.3",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "lighthouse": "^13.4.1",
    "yaml": "^2.9.1"
  }
}
